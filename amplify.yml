version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run lint
        - VITE_API_BASE_URL="${VITE_API_BASE_URL:-https://uwv0zhujxf.execute-api.ap-northeast-2.amazonaws.com}" npm run build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
